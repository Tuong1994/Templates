export enum EProductStatus {
    ACTIVE = 1,
    DRAFT = 2,
}

export enum EInventoryStatus {
    IN_STOCK = 1,
    OUT_OF_STOCK = 2,
}

export enum EProductType {
    BEST_SALE = 1,
    OUT_STAND = 2,
  }
  